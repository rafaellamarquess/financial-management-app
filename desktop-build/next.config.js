/** @type {import('next').NextConfig} */
const path = require('path');


const nextConfig = {
	// output: 'export',
	// distDir: 'dist',
	// webpack: (config, options) => {
	// 	config.entry = {electron: './electron/main.ts'}
	// 	config.module.rules.push( {
	// 		test: /\.node$/,
	// 		loader: "node-loader",
	// 	})
	// 	return config
	// }
}

module.exports = nextConfig
